# React Boilerplate

React boilerplate based on webpack.

This is used to test some personal projects.

- Webpack 5
- React Router
- Styling with CSS/SASS (including css modules)
- Lint rules based on airbnb code style
- Format and lint will be run before each commit
